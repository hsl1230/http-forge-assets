# Delay Response

Calls `GET /delay/3` which waits ~3 seconds before responding.

Use this to demonstrate per-request **timeout** settings. Lower the request
timeout in Settings below 3000ms to see the request fail with a timeout error.
